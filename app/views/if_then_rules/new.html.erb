<h1 class="text-xl font-semibold mb-6">
  If-Thenルール作成
</h1>

<%= form_with model: @if_then_rule_form, url: if_then_rules_path,class: "max-w-md space-y-6" do |f| %>
  <% if @if_then_rule_form.warnings.present? %>
  <p class="text-yellow-600">
    注意がありますが、保存できます。
  </p>

  <%= hidden_field_tag :ignore_warnings, true %>
<% end %>
  <%= f.hidden_field :memo_id %>

  <div class="form-control">
    <%= f.label :if_condition, "IF（きっかけ）", class: "label" %>
    <%= f.text_field :if_condition,
                     class: "input input-bordered",
                     placeholder: "例：朝7時に起きたら" %>
  </div>

  <div class="form-control">
    <%= f.label :then_action, "THEN（行動）", class: "label" %>
    <%= f.text_field :then_action,
                     class: "input input-bordered",
                     placeholder: "例：コップ一杯の水を飲む" %>
  </div>

  <div class="pt-4">
    <%= f.submit @if_then_rule_form.warnings.present? ?  "無視して保存する" : "保存する",
                 class: "btn btn-primary w-full" %>
  </div>
<% end %>