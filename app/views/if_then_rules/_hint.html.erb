<ul class="space-y-3 list-disc list-inside bg-white-100">
  <% warning_hints_for(target).each do |concept| %>
    <li class="list-none">
    <!--セルフチェック用として一応チェック-->
    <input type="checkbox" class="checkbox checkbox-info cursor-pointer checkbox-sm""  />
    <span class="ml-1 text-sm "><%= concept.definition.dig(:hint, :content) %></span>
    <span class="hint-label text-xs text-gray-400"><%= concept.definition[:label] %></span>
    </li>
  <% end %>
</ul>
